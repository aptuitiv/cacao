---
title: Margins - Variables
---

The size of the margins are set using CSS variables. You will need to either import the `cacao-css/dist/margin/variables.css` file into your CSS or set all of the variables with your own CSS.

You could import the margin variables file and still override one or more of the variables in your own code. You just want to make sure that the code that overrides the variables is set after this import.


## The variables

`:where()` is used to give the variables no specificity so that they are easily overriden. [See the MDN where() documentation for more information](https://developer.mozilla.org/en-US/docs/Web/CSS/:where).

The following variables are set.

| Variable | Value |
|----------|-------|
| --margin-0 | 0px |
| --margin-1 | 10px |
| --margin-2 | 20px |
| --margin-3 | 30px |
| --margin-4 | 40px |
| --margin-5 | 50px |
| --margin-6 | 60px |
| --margin-7 | 70px |
| --margin-8 | 80px |
| --margin-9 | 90px |
| --margin-10 | 100px |
| --margin-11 | 120px |
| --margin-12 | 160px |
| --margin-13 | 240px |
| --margin-14 | 320px |
| --margin-15 | 480px |

## How to import into your CSS

Import this into your CSS with:

```css
@import 'cacao-css/dist/margin/variables.css';
```

## Source CSS

<!-- markdownlint-disable -->
import CodeBlock from '@theme/CodeBlock';
import ImportsSource from '!!raw-loader!../../../../dist/margin/variables.css';

<CodeBlock language="css" title="margin/all.css">{ImportsSource}</CodeBlock>
